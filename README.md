# simple-ecom-website
Full-stack ecommerce site built with Python, React, HTML/CSS/JS.

To run the full backend locally:

1. Clone the repo  
2. Install Python 3.x  
3. Run: `pip install flask`  
4. Start server: `python backend/app.py`  
5. Open `http://localhost:5000` in your browser
